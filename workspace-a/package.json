{
  "name": "@yarn-monorepo/workspace-a",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rimraf dist && mkdir dist",
    "prebuild": "npm run clean && echo 'Getting rid of old workspace A stuff first 🔥'",
    "build": "eleventy && echo 'Building workspace A'"
  },
  "dependencies": {
    "@yarn-monorepo/global": "1.0.0"
  }
}
